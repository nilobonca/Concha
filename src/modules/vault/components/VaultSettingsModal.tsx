import React from 'react';

/**
 * VaultSettingsModal delega a exibição para o modal unificado de Opções (SettingsModal),
 * que escuta diretamente a propriedade `settingsOpen` da store do Vault e abre a aba "Vault & Armazenamento".
 */
export const VaultSettingsModal: React.FC = () => {
  return null;
};

